# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Database
DATABASE_URL="postgresql://postgres:postgres@db:5432/archer_fitness?schema=public"

# NextAuth Configuration
NEXTAUTH_SECRET="your-super-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (Optional)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# VAPID Keys for Push Notifications
# Generate these using: node -e "const webpush = require('web-push'); const keys = webpush.generateVAPIDKeys(); console.log('Public:', keys.publicKey); console.log('Private:', keys.privateKey);"
NEXT_PUBLIC_VAPID_PUBLIC_KEY="your-vapid-public-key-here"
VAPID_PRIVATE_KEY="your-vapid-private-key-here"
VAPID_EMAIL="admin@yourdomain.com"

# SMTP Configuration for Email Notifications (Optional)
# If you prefer email notifications over push notifications
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@yourdomain.com"
SMTP_PASS="your-app-password"
SMTP_FROM="your-email@yourdomain.com"

# Admin Email for Error and Startup Notifications
ADMIN_EMAIL="admin@yourdomain.com"

# Optional: Analytics
VERCEL_ANALYTICS_ID="your-vercel-analytics-id"

# Optional: Custom Port
PORT=3000

# Appwrite Storage (Optional - Progress Photos)
APPWRITE_ENDPOINT="https://cloud.appwrite.io/v1"
APPWRITE_PROJECT_ID="your-appwrite-project-id"
APPWRITE_API_KEY="your-appwrite-api-key"
APPWRITE_BUCKET_ID="your-appwrite-bucket-id"

# Progress photo encryption (server-wrapped key)
# 32-byte random key, base64-encoded
PHOTO_ENCRYPTION_MASTER_KEY="base64-encoded-32-byte-key"

# Optional: Key rotation support (comma-separated list, base64-encoded 32-byte keys)
# - First key is used for new encryption.
# - All keys are tried for decryption; if an older key is used successfully, the user key is re-wrapped with the first key.
# Example:
# PHOTO_ENCRYPTION_MASTER_KEYS="newKeyBase64,oldKeyBase64"
